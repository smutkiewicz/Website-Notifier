package com.smutkiewicz.pagenotifier;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

/**
 * Created by Admin on 2017-08-30.
 */

public class WebServiceReceiver extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent intent) {
        /*if(intent.getAction().equals(WebValuesService.ACTION_WEBSITE_UPDATED)) {
            //intent.getExtras().getString(WebValuesService.WEBSSITE_ID)
        }*/
    }
}
